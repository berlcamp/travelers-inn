node_modules/
.next/
out/
.env*.local
.env.local.*.bak
supabase/.temp/
supabase/.branches/
tsconfig.tsbuildinfo
*.log
.DS_Store

# test/tooling artifacts
.playwright-mcp/
login-page.png

*.png

# ...but brand assets are shipped files, not artifacts. Without these the logo
# is absent from every deploy while still rendering locally.
!public/*.png
!src/app/icon.png
!src/app/apple-icon.png
